
#ifndef __CELLMON_H
#define __CELLMON_H

#include "client.h"

#if 0
struct cellmon_battery {
	char name[SOLARD_AGENT_NAME_LEN+1];
	char uuid[38];
//	solard_battery_t *bp;
	int ntemps;
	int status;
	float capacity;
	float voltage;
	float current;
	float temps[BATTERY_MAX_TEMPS];
	int ncells;
	float cellvolt[BATTERY_MAX_CELLS];
	time_t last_update;
	uint16_t state;
};
typedef struct cellmon_battery cellmon_battery_t;
#endif

struct cellmon_config {
	solard_client_t *c;
	int cells;
	list packs;
	int state;
};
typedef struct cellmon_config cellmon_config_t;

#define BATTERY_MAX_PACKS 32

#endif
